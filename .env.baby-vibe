# Firebase framework-aware Hosting runs `next build` directly.
# Next.js 16 defaults to Turbopack, which the legacy Hosting adapter cannot package correctly.
IS_WEBPACK_TEST=1
NEXT_PUBLIC_APP_URL=https://baby-vibe.web.app
